# v80 非线性玻璃反馈与预测返回修正

- 预测式返回视觉层改为单一的水平向右平移 + 透明淡出，移除所有中心缩放、裁切、阴影叠加。
- Dock 液态玻璃 lens 的宽高、边缘光、折射、放大倍率通过 TweenAnimationBuilder + easeOutBack/elasticOut 过渡，不再直接跳变。
- 加号 lens、新增/编辑保存按钮、心愿保存按钮、取消按钮改为非线性按压过渡。
- 液态模式下继续保持非交互 pixelRatio=1.3，交互时 pixelRatio=1.0。
- 毛玻璃/普通按钮保留 0.79 风格，但触控时也有非线性放大/回弹反馈。
